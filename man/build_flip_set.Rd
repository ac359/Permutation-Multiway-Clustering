% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signflip.R
\name{build_flip_set}
\alias{build_flip_set}
\title{Construct a random sign-flip group (the sign-flip analogue of Algorithm 1)}
\usage{
build_flip_set(n_row, n_col, n_flip, seed = NULL, cells = NULL)
}
\arguments{
\item{n_row, n_col}{Integer cluster counts along the two dimensions.}

\item{n_flip}{Integer, the number of flip groups, at least 2 and at most
\code{n_row + n_col} (so that every group can be reached). Group order is
\code{2^(n_flip - 1)}, and the cost of the test is one residual projection
per element, so keep it moderate: \code{\link{mwperm_dyadic_het}} defaults
to the smallest number of flip groups whose p-value floor
\code{1 / 2^(n_flip - 1)} (doubled under \code{aggregate = "median2"}) is
at most \code{alpha} -- 6 at \code{alpha = 0.05} -- and refuses values
above 20; 6-8 are the useful range, and the cost doubles with each extra
group.}

\item{seed}{Optional integer seed for the random assignment. If \code{NULL},
the current RNG state is used. A seeded call leaves the caller's RNG
stream exactly as it found it.}

\item{cells}{Optional integer matrix with two columns, the \code{(row, col)}
ids (in \code{1..n_row}, \code{1..n_col}) of the cells that are actually
observed; \code{NULL} (the default) means the complete array. On an
incomplete array a flip group can be reachable only through cells that are
all missing, so "every group used" no longer guarantees that the
\code{2^(n_flip - 1)} representatives are distinct; the assignment is then
redrawn until the graph joining a row group to a column group whenever some
\emph{observed} cell has that pair is connected (exactly the condition for
the kernel to be \code{{s, -s}} on the observed cells). On a complete array
that condition is implied by "every group used", so passing the full cell
set changes nothing.}
}
\value{
A list of length \code{2^(n_flip - 1)}. Element \code{k} is a list with
  two numeric \code{+1/-1} vectors, \code{row} (length \code{n_row}) and
  \code{col} (length \code{n_col}): the transformation multiplies
  observation \code{(i, j)} by \code{row[i] * col[j]}. The first element is
  the identity (all \code{+1}). Attributes: \code{"n_flip"},
  \code{"group_order"} (\code{2^(n_flip - 1)}), \code{"row_groups"} and
  \code{"col_groups"} (the two integer assignments, length \code{n_row} and
  \code{n_col}), and \code{"signs"} (the \code{2^(n_flip - 1)} by
  \code{n_flip} matrix of coset representatives, one row per element, row 1
  all \code{+1}).
}
\description{
Builds the \code{2^(n_flip - 1)} distinct sign-flip transformations of an
\code{n_row} by \code{n_col} array that \code{\link{mwperm_dyadic_het}}
uses as its invariance group. Each row cluster and each column cluster is
assigned, independently and uniformly at random, to one of \code{n_flip}
\emph{flip groups}; a sign vector \code{s} in \code{{-1, +1}^n_flip} then
multiplies observation \code{(i, j)} by \code{s[g1(i)] * s[g2(j)]}, the
product of its row group's sign and its column group's sign. The returned
set is closed under composition (composition is the coordinate-wise product
of sign vectors), contains the identity exactly once, and has exactly
\code{2^(n_flip - 1)} elements.
}
\details{
\strong{Why \code{2^(n_flip - 1)} and not \code{2^n_flip}.} Because the
sign enters as a \emph{product} of a row sign and a column sign, \code{s}
and \code{-s} induce the identical transformation: the action has a kernel
of order 2, so the \code{2^n_flip} sign vectors produce only
\code{2^(n_flip - 1)} distinct transformations, each of them twice. This
function enumerates one representative per pair (coordinate 1 fixed to
\code{+1}), which is exact: the p-value is bit-identical to what the full
enumeration would give, at half the cost. The effective group order is
therefore \code{2^(n_flip - 1)} and the smallest attainable p-value
\code{1 / 2^(n_flip - 1)}, so a 95\% confidence set needs
\code{n_flip >= 6}.

\strong{Every flip group must be used.} If some group were assigned to no
row cluster and no column cluster its sign would be free, the kernel would
be larger than 2, and the \code{2^(n_flip - 1)} representatives would
contain duplicates -- a valid test whose \emph{reported} resolution would
be wrong. The assignment is therefore resampled until the union of the row
and column images covers all \code{n_flip} groups. That is rare at small
\code{n_flip} (about 5\% of draws at \code{n_flip = 10} with 25 clusters
per side) but it must be guaranteed, not left to chance.

The randomness in the assignment is what makes the resulting test a
\emph{random} invariant test, exactly as the relabelling does in
\code{\link{build_perm_set}}: different seeds give slightly different
p-values, and the \code{n_reps} argument of \code{\link{mwperm_dyadic_het}}
aggregates several draws. The same reproducibility caveats apply (same
\code{RNGkind()}; see \code{\link{build_perm_set}}).
}
\examples{
F <- build_flip_set(n_row = 6, n_col = 5, n_flip = 4, seed = 1)
length(F)                  # 2^(4 - 1) = 8 distinct sign patterns
F[[1]]                     # identity: all +1
attr(F, "row_groups")      # which flip group each row cluster belongs to
attr(F, "group_order")
## the sign applied to cell (i, j) by element 3
outer(F[[3]]$row, F[[3]]$col)
}
\references{
Guo, W., Toulis, P. and Wang, Y. (2026). Permutation inference under
  multi-way clustering and missing data. arXiv:2601.08610. Procedure 1
  under the double sign symmetry assumption (Assumption 2 of the revised
  paper).
}
\seealso{
\code{\link{mwperm_dyadic_het}}, the test built on this group;
  \code{\link{build_perm_set}} for the permutation group the other designs
  use.
}
