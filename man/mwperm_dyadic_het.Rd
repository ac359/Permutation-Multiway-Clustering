% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signflip.R
\name{mwperm_dyadic_het}
\alias{mwperm_dyadic_het}
\title{Sign-flip invariant test for dyadic regression (heteroskedasticity-robust)}
\usage{
mwperm_dyadic_het(
  y,
  d,
  x = NULL,
  row,
  col,
  n_flip = NULL,
  alpha = 0.05,
  beta_null = 0,
  conf_int = TRUE,
  n_reps = 10L,
  seed = NULL,
  grid = NULL,
  aggregate = c("median", "median2"),
  n_cores = 1L
)
}
\arguments{
\item{y}{Numeric outcome vector, one entry per observed cell.}

\item{d}{Numeric vector or matrix of the covariate(s) of interest
(d_ij). With a single covariate a confidence interval is produced;
with several, a joint confidence region (see \code{conf_int}).}

\item{x}{Optional numeric matrix or data frame of nuisance covariates
(x_ij); an intercept is always added internally. May be
\code{NULL}.}

\item{row, col}{Vectors giving the row-cluster and column-cluster identity of
each observation (any type coercible by \code{\link{factor}}).}

\item{n_flip}{Number of flip groups. The group has order
\code{2^(n_flip - 1)}, so the smallest attainable p-value is
\code{1 / 2^(n_flip - 1)} and a 95\% confidence set needs
\code{n_flip >= 6}. Defaults to 8, capped at \code{min(n_row, n_col)}
(every group must be reachable by the row assignment alone); values above
20 are refused, with the number of projections they would imply. Must
satisfy \code{2 <= n_flip <= min(n_row, n_col)}.}

\item{alpha}{Significance level used for the reject/retain decision and for
the inverted confidence interval (\code{conf_level = 1 - alpha}).}

\item{beta_null}{Null value b to test. Scalar (recycled if \code{d}
has several columns).}

\item{conf_int}{Logical; if \code{TRUE} a confidence set is computed by test
inversion -- the set of null values the test does not reject, exactly as
Procedure 1 step 3 defines it. For a single covariate the set is computed
\emph{exactly} (the p-value is a step function of the null value, and the
package evaluates it at every jump), and its connected components are
returned in the \code{conf_set} field with their hull in \code{conf_int};
for several covariates, a joint (grid-based) confidence region. See
\code{\link{confint.mwperm}}.}

\item{n_reps}{Number of independent runs whose p-values are aggregated by
\code{aggregate} (the median by default), as recommended for randomised
tests; the confidence set inverts the same aggregated p-value.
Defaults to 10: a single run's p-value depends on the random assignment
of clusters to flip groups (a seed lottery), and the median of 10 runs
stabilises it at roughly ten times the cost. See \emph{Aggregation over
repetitions} in \code{\link{mwperm_dyadic}}.}

\item{seed}{Optional integer; if supplied, run \code{r} uses seed
\code{seed + r - 1} for reproducibility.}

\item{grid}{Optional candidate beta values for the confidence set. For
a single covariate, a numeric vector: the interval becomes the hull of the
grid points not rejected by the median-aggregated test (the same
de-randomisation as the reported p-value; an acceptance region reaching a
grid edge is reported as unbounded on that side). For several, a list of
one numeric vector per covariate defining the region search grid.}

\item{aggregate}{How the \code{n_reps} per-repetition p-values are combined
into the reported p-value, and into the confidence set that inverts it.
\code{"median"} (the default) is the median, as recommended in Remark 1 of
Guo, Toulis and Wang (2026); \code{"median2"} is \code{min(1, 2 * median)},
a valid p-value at level \code{alpha} under arbitrary dependence across
repetitions, at the cost of resolution: its floor is
\code{2 / 2^(n_flip - 1)}, so a 95\% set then needs \code{n_flip >= 7}.
See \code{\link{mwperm_dyadic}} for the full account.}

\item{n_cores}{Number of CPU cores (default 1 = serial). Parallelism is over
the \code{n_reps} repetitions when several are run with a \code{seed},
otherwise over the \code{2^(n_flip - 1) - 1} per-element projections;
either way the result is \emph{identical} to the serial one. See
\code{\link{mwperm_dyadic}}.}
}
\value{
An object of class \code{"mwperm"} exactly as \code{\link{mwperm_dyadic}}
  returns it (see there for the provenance of every field), with
  \code{type} equal to \code{"dyadic (sign-flip / heteroskedasticity-robust)"},
  \code{K} and \code{n_perm} equal to \code{2^(n_flip - 1) - 1} and
  \code{2^(n_flip - 1)} (the non-identity count and the group order, the
  roles those fields play for every design), and an extra field
  \code{n_flip}. All the methods -- \code{\link{print.mwperm}},
  \code{\link{summary.mwperm}}, \code{\link{confint.mwperm}},
  \code{\link{plot.mwperm}} -- apply.
}
\description{
Finite-sample valid test of H0: beta = b in the dyadic regression model

\preformatted{  y_ij = x_ij' gamma + d_ij' beta + eps_ij}

with one observation per cell (the array need not be complete), under an
invariance assumption that tolerates \strong{arbitrary heteroskedasticity}
but \strong{not additive cluster effects}: the error array must be
\emph{jointly} symmetric under row-and-column sign changes,

\preformatted{  (eps_ij) =d (s_i t_j eps_ij) | X, D   for all row signs s and column signs t.}

This is the invariant test of Guo, Toulis and Wang (2026) run with a
\emph{sign-flip} group in place of the permutation group -- Section 2 of
the paper notes that the partialling-out and minorization argument holds
for any invariance group -- and is the test the authors call IPT-Het. Same
model, same null, same statistic, same confidence set by test inversion;
only the group changes, and with it the assumption.
}
\details{
\strong{What the assumption does and does not allow.} A sign flip moves no
observation and changes no variance, so the variance \code{sigma_ij} may
depend on \code{i}, on \code{j} and on the covariates in any way. The
assumption holds for errors that are \strong{independent across cells} and
symmetric about zero (\code{eps_ij = sigma_ij u_ij} with \code{u_ij}
independent symmetric, \code{sigma_ij} arbitrary), and more generally for
errors whose dependence across cells is itself sign-symmetric
(multiplicative cluster factors, \code{eps_ij = a_i b_j u_ij} with
\code{a_i, b_j} symmetric). It does \strong{not} hold under the additive
random-effects structure that motivates multi-way clustering,
\code{eps_ij = eta_i + xi_j + u_ij}: flipping the sign of one row turns
\code{Cov(eps_ij, eps_kj) = Var(xi_j)} (k another row) into its negative,
so the joint law changes even though every single error is symmetric. Under
that structure
this test \strong{over-rejects} -- about 0.12-0.15 at a nominal 0.05 when
\code{d} carries a row-level component, in this package's own checks (the
permutation test held 0.04-0.05 on the same data) -- and symmetry of each
error on its own is not enough. Skewed errors are excluded as well.

\strong{When to use it instead of \code{\link{mwperm_dyadic}}.} The two
assumptions are complementary and neither contains the other:
\itemize{
  \item \code{\link{mwperm_dyadic}} needs the error array to be
  \emph{exchangeable} under relabelling of the clusters, conditional on the
  covariates. That tolerates additive cluster effects and any error
  distribution, but fails whenever the error variance (or any other feature
  of the error law) depends on the covariates -- a gravity equation whose
  residual variance grows with distance or GDP, say -- because relabelling
  the clusters relabels the variance pattern. Symmetry is \emph{not}
  required.
  \item \code{mwperm_dyadic_het()} tolerates any covariate-driven
  heteroskedasticity but needs the joint sign symmetry above: independent
  (or sign-symmetrically dependent) symmetric errors, \strong{no additive
  cluster effects}.
}
The authors' own design for this test (\code{simulate_gravity_model()} in
their revision material) draws errors that are independent across cells
with a variance increasing in the gravity mean and in distance; that is the
setting it is meant for.

Under covariate-dependent heteroskedasticity (25 clusters per side, a
gravity design with the residual variance increasing in the mean and in the
distance covariate) the permutation test's rejection rate under a true null
rose with the strength of the heteroskedasticity -- to about 0.06-0.09 at
a nominal 0.05 in this package's own 300-replication check, and to about
0.13 in the authors' -- while this test stayed at or below nominal (0.02
and 0.05 respectively). That gap is the reason the function exists. It is
\strong{not a strict upgrade}: under homoskedastic, exchangeable errors the
sign-flip test is less powerful (about 0.86 against 0.96 at beta = 0.15 in
the same design here; 0.90 against 0.98 in the authors' run), because its
group is smaller and coarser than a full relabelling group. Use it when the
errors are plausibly independent across cells with a variance that depends
on the covariates; use \code{\link{mwperm_dyadic}} when additive cluster
effects are the concern. Heteroskedasticity leaves no trace in the
clustering structure, so \code{\link{mwperm}} never selects this test
automatically: request it with \code{design = "dyadic_het"}.

\strong{Incomplete arrays.} No fully observed block is needed: every
observed cell is used and nothing is discarded (the fit says so in a note).
The only thing an incomplete array changes is the guard on the flip-group
assignment, which must keep the group's kernel at \code{{s, -s}} on the
observed cells -- see the \code{cells} argument of
\code{\link{build_flip_set}}.

\strong{The group and its resolution.} Each row cluster and each column
cluster is assigned at random to one of \code{n_flip} flip groups, and a
sign vector in \code{{-1, +1}^n_flip} multiplies observation \code{(i, j)}
by the product of its row group's sign and its column group's sign (see
\code{\link{build_flip_set}}). A sign vector and its negative induce the
same transformation, so the group has \code{2^(n_flip - 1)} distinct
elements, not \code{2^n_flip}; the package enumerates each exactly once.
Per repetition the p-value therefore lives on the grid
\code{{1, ..., 2^(n_flip - 1)} / 2^(n_flip - 1)}, its smallest value is
\code{1 / 2^(n_flip - 1)}, and a \code{(1 - alpha)} confidence set needs
\code{2^(n_flip - 1) >= 1 / alpha}: at \code{alpha = 0.05} that is
\code{n_flip >= 6} (order 32). Below that the p-value is still exact but no
set is attainable, and the fit says so in a note. The cost is one residual
projection per non-identity element -- \code{2^(n_flip - 1) - 1} per
repetition, exponential in \code{n_flip} -- which is why the default is a
fixed \code{n_flip = 8} (order 128, floor 0.0078, 127 projections) rather
than the largest value the design allows, as \code{K} is for the
permutation tests. At that default with \code{n_reps = 10} the exact
confidence set exceeds the engine's candidate budget and the interval is
found by bracketing and bisection instead (the fit says so in a note; the
end points agree to the bisection tolerance); \code{n_reps <= 6}, or
\code{n_flip <= 7}, keeps it exact.

The aggregation over \code{n_reps} repetitions, the exact confidence set
and its closure convention, the \code{aggregate = "median2"} guarantee and
the numerical notes on the projection are all exactly as documented in
\code{\link{mwperm_dyadic}}; the sign-flip element \code{S_k} simply takes
the place of the permuted copy in the stacked design \code{[X | S_k X]}.
}
\examples{
data(trade_dyadic)
## the same call as the mwperm_dyadic() example, under sign symmetry
## instead of exchangeability
fit <- with(trade_dyadic,
            mwperm_dyadic_het(y = log_trade, d = log_dist,
                              x = cbind(log_gdp_i, log_gdp_j),
                              row = importer, col = exporter,
                              n_flip = 6, n_reps = 3, seed = 1))
fit
confint(fit)
}
\references{
Guo, W., Toulis, P. and Wang, Y. (2026). Permutation inference under
  multi-way clustering and missing data. arXiv:2601.08610.
}
\seealso{
\code{\link{mwperm_dyadic}} for the permutation test under
  exchangeability, the test to compare this one against;
  \code{\link{build_flip_set}} for the group construction;
  \code{\link{mwperm}} with \code{design = "dyadic_het"}.
}
