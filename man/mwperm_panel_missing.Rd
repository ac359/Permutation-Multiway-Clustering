% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_missing.R
\name{mwperm_panel_missing}
\alias{mwperm_panel_missing}
\title{Invariant permutation test for incomplete panels}
\usage{
mwperm_panel_missing(
  y,
  d,
  x = NULL,
  row,
  col,
  time,
  K = NULL,
  min_block = 3L,
  block_method = c("greedy", "exact"),
  alpha = 0.05,
  beta_null = 0,
  conf_int = TRUE,
  n_reps = 10L,
  seed = NULL,
  grid = NULL,
  time_fe = TRUE,
  aggregate = c("median", "median2"),
  n_cores = 1L
)
}
\arguments{
\item{y}{Numeric outcome vector.}

\item{d}{Numeric vector or matrix of covariate(s) of interest. With a single
covariate a confidence interval is returned; with several, a joint
acceptance region.}

\item{x}{Optional numeric matrix or data frame of nuisance covariates. An
intercept is added automatically.}

\item{row, col}{Cluster identifiers of the two cross-sectional dimensions,
for example importer and exporter. Any type; recoded to dense ids.}

\item{time}{Period identifier. Held fixed by the permutation -- the errors
are never assumed exchangeable across it.}

\item{K}{Number of non-identity permutations; defaults to
\code{min(smallest block side) - 1} capped at 199. Must satisfy
\code{K + 1 <=} the smallest side of the smallest selected block.}

\item{min_block}{Minimum block side for the biclique search, a single
integer or a length-2 integer giving the row and column floors separately.
Floored at 2.}

\item{block_method}{\code{"greedy"} (default) or \code{"exact"}; see
\code{\link{find_bicliques}}. A sub-maximal block costs power, never
validity.}

\item{alpha}{Significance level used for the confidence set and the printed
decision.}

\item{beta_null}{Null value(s); length 1 or \code{ncol(d)}.}

\item{conf_int}{Logical; compute the confidence set by test inversion.}

\item{n_reps}{Number of independent permutation groups; the reported
p-value is aggregated over them.}

\item{seed}{Integer seed. Required for reproducibility, and for the
rep-parallel path.}

\item{grid}{Optional explicit grid of null values for the inversion.}

\item{time_fe}{Logical; if \code{TRUE} (the default) period dummies are
added to the nuisance design. They are invariant to the within-period
permutation, so they cost no validity, and they remove the common time
effect from the point estimate.}

\item{aggregate}{Cross-repetition rule, \code{"median"} (default) or
\code{"median2"}.}

\item{n_cores}{Integer; 1 (default) runs serially and is bit-identical to
any larger value.}
}
\value{
An object of class \code{"mwperm"}. Beyond the usual fields it carries
\code{n_blocks}, \code{cells_used} and \code{cells_total}, the block
accounting.
}
\description{
Finite-sample valid test of H0: beta = b for a panel
\preformatted{  y_ijt = x_ijt' gamma + d_ijt' beta + eps_ijt}

in which the (i, j) array is \strong{incomplete} -- some pairs are never
observed, or are observed in only some periods -- so
\code{\link{mwperm_panel}}, which requires a complete balanced array, cannot
run. This combines condition InvB of Guo, Toulis and Wang (2026), Section
6.2 with the missing-data machinery of Section 5, which is the extension
their Section 9 leaves open.
}
\details{
The construction is:
\enumerate{
\item form the mask \code{M_ij = 1} if pair (i, j) is observed in EVERY
period, and 0 otherwise;
\item run the biclique search (\code{\link{find_bicliques}}, Algorithm 2) on
that mask to obtain disjoint fully observed blocks \code{I_q} by \code{J_q};
\item discard every observation outside the selected blocks;
\item apply Procedure 2 to what remains: a row group on \code{I_q} and a
column group on \code{J_q} per block, of common order K + 1, applied
identically in every period, so cell (i, j) in period t maps to cell
(pi(i), sigma(j)) in period t.
}

Holding the period fixed is the same device \code{\link{mwperm_panel}} uses,
and it is what keeps the test valid when the errors are autocorrelated over
time.
}
\section{Assumptions}{

Two conditions, and neither is within-period independence. The first is
condition InvB applied blockwise: for every period t, and with the
\emph{same} (pi, sigma) used in every period,

(eps_ijt for i in I_q, j in J_q) has the same distribution, given X and D,
as (eps_[pi(i)][sigma(j)]t for i in I_q, j in J_q)

The second is Assumption 4 on the mask: which pairs are completely observed
is independent of the errors given the covariates. The mask may depend on
the covariates in any way -- an unbalanced panel where richer country pairs
are observed in more years is fine -- but not on the outcome. Under those
two conditions the p-value is exact in finite samples, by the argument of
Theorem 4 applied within each block.

Arbitrary common time effects are permitted, exactly as in
\code{\link{mwperm_panel}}: nothing is assumed across periods, so a trend, a
break, or serial correlation of any form costs no validity.
}

\section{What is discarded}{

A pair observed in some periods but not all fails the mask and is dropped
whole, and pairs that clear the mask are still dropped if the biclique
search cannot place them in a block. That loss is the price of exactness
under missingness, and it is reported in \code{note}, \code{cells_used} and
\code{cells_total}. Dropping \emph{periods} instead -- keeping only the
years in which every pair is observed -- is often the better trade and is
done by subsetting the data before calling this function; there is no free
lunch, and which side to cut depends on the panel.
}

\examples{
data(trade_panel)
## thin 20 country pairs so they are observed in the first year only:
## an unbalanced panel, which mwperm_panel() cannot test
set.seed(1)
tp <- trade_panel
pair <- paste(tp$importer, tp$exporter)
thin <- sample(unique(pair), 20)
tp <- tp[!(pair \%in\% thin & tp$year > min(tp$year)), ]
fit <- with(tp, mwperm_panel_missing(y = log_trade, d = fta,
                                     x = cbind(log_gdp_i, log_gdp_j),
                                     row = importer, col = exporter,
                                     time = year, min_block = 5, seed = 1))
fit
## Note what the fit reports: 20 thinned pairs cost far more than 20 cells,
## because a block must be complete in BOTH margins. With 22 clusters per
## side an incomplete panel rarely supports a 95\% interval -- K + 1 >= 20
## needs a fully observed 20 x 20 block. Dropping the sparsest periods
## before calling is usually the better trade.
}
\references{
Guo, W., Toulis, P. and Wang, Y. (2026). Permutation inference under
multi-way clustering and missing data. Condition InvB is Section 6.2,
Procedure 2 and Assumption 4 are Section 5. arXiv:2601.08610.
}
\seealso{
\code{\link{mwperm_panel}} for a complete balanced panel,
\code{\link{mwperm_missing}} for an incomplete array with no time dimension,
and \code{\link{find_bicliques}} for the block search.
}
